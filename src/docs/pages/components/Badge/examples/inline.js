import React from 'react';
import { Badge, Text } from '@barry127/bui';

const Demo = () => (
  <React.Fragment>
    <Text inline>Inbox </Text>
    <Badge count={12} />
  </React.Fragment>
);

export default Demo;
